This scientific paper adresses basic deep networks architecture (MLP, CNN) and regularisations methods such as early stopping, dropout.
Experiments were made on a Traffic Sign Dataset, containing 52 classes. 
